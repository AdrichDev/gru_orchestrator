{
  "name": "@gru/whatsapp-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Thin chat gateway (WhatsApp via Kapso + Telegram) that injects messages into the Gru orchestrator intake. Chat is only an I/O channel; all routing, risk and escalation stay in Gru.",
  "main": "src/index.ts",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kapso/whatsapp-cloud-api": "latest",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
